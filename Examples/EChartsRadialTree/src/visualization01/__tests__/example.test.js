/**
 * @jest-environment jsdom
 */

// Put your JEST tests in here.

describe('Simple test', () => {  
    it('works', () => {
      expect(0).toBe(0)
    })
    // it('fails', () => {
    //   expect(1).toBe(0)
    // })
  })
  